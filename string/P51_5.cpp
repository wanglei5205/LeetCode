#include<iostream>
using namespace std;

struct ListNode {
    int val;
    struct ListNode *next;
    ListNode(int x) :
          val(x), next(NULL) {
      }
};

// 从尾到头打印链表
vector<int> printListFromTailToHead(ListNode* head)
{

}
int main()
{
    PrintList();
    return 0;
}
